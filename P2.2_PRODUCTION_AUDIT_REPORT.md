# P2.2 Production Audit Report — Commit 520a045

**Audit Date**: 2026-07-20
**Auditor**: Hermes Agent (Agnes)
**Commit**: `520a045` — P2.2: Blind School Engine + Evidence Library
**Verdict**: ❌ **HOLD — 暂不准入**

---

## 一、审计摘要

| 检查项 | 状态 | 严重程度 |
|--------|------|---------|
| Evidence Authority 晋级链 | ❌ 失败 | 🔴 CRITICAL |
| Self-Attestation 违规 | ❌ 失败 | 🔴 CRITICAL |
| Fidelity 状态不一致 | ❌ 失败 | 🔴 HIGH |
| Residual Notes 污染 | ⚠️ 部分 | 🟡 MEDIUM |
| Confidence 字段违规 | ❌ 发现 | 🔴 HIGH |
| Judgment 直接输出 | ⚠️ 需进一步检查 | 🟡 MEDIUM |
| 6039行删除是否误伤 | ⚠️ 需核查 | 🟡 MEDIUM |
| Engine 架构合规性 | 🟢 基本通过 | — |

---

## 二、Critical Findings

### 2.1 Evidence Authority 非法晋级链

**问题描述**：commit 将 75 个证据从 `UNVERIFIED/PENDING/REJECTED` 批量提升为 `VERIFIED`，但未满足 Production Authority 证明标准。

**数据证据**：

```bash
# 状态变化统计
BEFORE (520a045^):  authority_status = UNVERIFIED/PENDING/REJECTED
AFTER  (520a045):   authority_status = VERIFIED (75/76)

# 验证者分析
verifier = "Hermes Agent (Agnes)"           → 75 files
verifier = "Hermes Agent (Agnes) + Blind BOT" → 73 files
verifier = null                              → 1 file
```

**核心违规**：
> 生产 Evidence 不能因为"生成/整理它的 Agent 自己验证了它"，就获得 authority。
> 这形成 self-attestation 循环：Agent 提取 → Agent 判断正确 → Agent 标 VERIFIED → Runtime 信任。

**示例对比**（E-BLIND-A-GUEST_HOST-001）：

| 字段 | Before | After |
|------|--------|-------|
| authority_status | `UNVERIFIED` | `VERIFIED` |
| source_fidelity | `REJECTED` | `ACCEPTED` |
| source_verification.status | `REJECTED` | `VERIFIED` |
| source_verification.reason | `SOURCE_UNVERIFIABLE` | `SOURCE_EXCERPT_FOUND` |
| source.title | `夏仲奇卜命遗例集` | `段建业《盲派初级命理学》` |
| verifier | `Hermes Agent` | `Hermes Agent (Agnes) - 最终核实` |

**关键问题**：
1. `SOURCE_UNVERIFIABLE` → `SOURCE_EXCERPT_FOUND` 是**人工判断**，非自动可验证
2. 来源从"夏仲奇遗例集"替换为"段建业《盲派初级命理学》"——这是**来源替换（source substitution）**，不是验证
3. 验证者仍是 Agent 自己

---

### 2.2 Fidelity 状态与 Authority 不一致

**统计数据**：

```
authority_status = VERIFIED:          75 files
source_fidelity = ACCEPTED:           2 files  ← 仅2个文件一致性正确
source_fidelity = PENDING_VERIFICATION: 73 files ← 73个文件矛盾
```

**违规模式**：

```json
{
  "authority_status": "VERIFIED",      // ← 声称已验证
  "source_fidelity": "PENDING_VERIFICATION", // ← 但保密度仍在待定
  "source_verification": {
    "status": "VERIFIED",              // ← 自己说已验证
    "verifier": "Hermes Agent (Agnes)" // ← 自己验证自己
  }
}
```

**影响**：系统如果只读取 `authority_status`，会误认为这些证据已达到 Production Authority。

---

### 2.3 Residual Notes 污染

**统计数据**：
- 53/76 文件 notes 字段仍包含旧验证标记：`DOWNGRADED`、`强制修正`
- 这些标记表明证据曾经历验证降级，但现在被反向提升

**示例**（E-BLIND-BODY_USE_RELATION-002）：

```json
{
  "notes": "... | ⚠️ 理论层与实际概念层不一致，需人工复核 | ... | DOWNGRADED ... | CANDIDATE→VERIFIED ..."
}
```

**风险**：后续审计工具可能错误解析 notes 中的历史状态。

---

### 2.4 Confidence 字段违规

**发现位置**：`src/tongshu/engines/blind/palace.py`

```python
@dataclass(frozen=True)
class PalaceRule:
    palace: str
    semantics: List[str]
    source: str
    confidence: float = 1.0  # ← 置信度字段，违反禁分原则
```

**使用情况**：
- 第 93 行：`confidence=item.get("confidence", 1.0)`
- 第 104-122 行：硬编码 `confidence=0.9`、`confidence=0.95`
- 第 204 行：输出 `"confidence": r.confidence`

**违反规则**：
> 盲派不能引入置信度/评分/投票机制（来自 memory 约束）

---

## 三、Medium Findings

### 3.1 Judgment 字段存在于 Rule 模型

**发现位置**：`src/tongshu/engines/blind/rules/models.py`

```python
@dataclass(frozen=True)
class Rule:
    rule_id: str
    school: str
    requires: List[str]
    invalidates: List[str]
    relations: List[str] = field(default_factory=list)
    judgment: str = ""  # ← 判断结论
    evidence_refs: List[str] = field(default_factory=list)
```

**风险评估**：
- 如果 `judgment` 直接在 Engine 中输出，则绕过 Rule/Assertion/Authority 层
- 需要确认：`judgment` 是用于元数据记录，还是直接产生断言

**建议**：在 `rules/graph.py` 中确认 `judgment` 的使用路径。

### 3.2 来源替换（Source Substitution）未独立审计

**现象**：
- `E-BLIND-A-GUEST_HOST-001`：来源从"夏仲奇遗例集"替换为"段建业《盲派初级命理学》"
- `E-BLIND-A-BODY_USE-001`：同上

**问题**：
- 来源替换改变了证据的 provenance chain
- 原 claim（夏仲奇口传）与新 source（段建业书面）的语义等价性未经独立审计
- 文档说明这是"段建业系统整理"，但整理 ≠ 原文

**建议**：所有来源替换必须：
1. 记录替换原因
2. 验证新旧 source 的语义等价性
3. 由独立于 Agent 的 authority 确认

---

## 四、已验证通过的项目

### 4.1 Engine 架构方向

Blind Engine 的架构设计符合系统要求：

```
FrozenBaziState
      ↓
Blind Feature Layer (palace, host_guest, body_use)
      ↓
Blind Rule Graph (rules/graph.py, matcher.py)
      ↓
EvidenceProducer → EvidenceList
```

**关键合规点**：
- `evidence_producer.py` 明确注释"不产生 direction/polarity/strength/confidence"
- `BlindFeatureState` 是 frozen dataclass，只包含结构事实
- Engine 不直接输出 Judgment

### 4.2 WorkGraph / WorkChain 设计

- `workgraph.py`：定义节点类型（Stem/Branch/TenGod/Palace/Body/Use）和边类型（生/克/制/化/合/冲）
- `workchain.py`：查找体到用的有向路径

这两者只是关系图结构，不产生判断。

---

## 五、被删除文件分析

### 5.1 删除的 metadata 文件（9个）

```
manifest.json                    (-51 lines)
provenance_final_status.json     (-12 lines)
provenance_rollback_log.json     (-8 lines)
provenance_rules.json            (-106 lines)
provenance_validation_report.json (-20 lines)
reclassification_matrix.json     (-2235 lines)
source_verification_final_report.json (-2201 lines)
source_verification_report.json  (-123 lines)
source_verification_results.json (-496 lines)
topic_correction_log.json        (-40 lines)
```

**总计删除**：~5,292 行

**风险评估**：
- 这些是证据库的元数据和报告文件，非生产代码
- `provenance_rules.json` 可能包含验证规则定义，需确认是否被其他模块引用
- `reclassification_matrix.json` 是重分类矩阵，属临时文件

**建议**：检查这些文件是否被其他模块 import 或引用。

### 5.2 保留的核心文件

- 76 个 `E-BLIND-*.json` 证据文件
- `blind/` 引擎代码（新增 + 修改）
- tests

---

## 六、审计结论

### 6.1 问题分类

| 类别 | 问题数 | 严重程度 |
|------|--------|---------|
| Evidence Authority 非法晋级 | 75 files | 🔴 CRITICAL |
| Self-Attestation | 75 files | 🔴 CRITICAL |
| Fidelity 不一致 | 73 files | 🔴 HIGH |
| Confidence 字段违规 | 1 file (palace.py) | 🔴 HIGH |
| Residual Notes 污染 | 53 files | 🟡 MEDIUM |
| 来源替换未审计 | 2 files | 🟡 MEDIUM |
| Judgment 使用路径待查 | 1 file (models.py) | 🟡 MEDIUM |

### 6.2 裁决

```
┌─────────────────────────────────────────────────────────────┐
│  Commit 520a045 裁决：HOLD                                  │
│                                                             │
│  ✅ 允许继续：Blind Engine 架构代码（palace/workchain等）     │
│  ❌ 禁止合并：Evidence 状态批量提升                          │
│  ❌ 禁止合并：Self-attestation verification                   │
│  ❌ 禁止合并：Confidence 字段                                 │
│                                                             │
│  🔄 必须修复后再审：                                         │
│    1. 证据降级回 UNVERIFIED/PENDING，等待独立人工验证         │
│    2. 移除 PalaceRule.confidence 字段                        │
│    3. 清理 residual notes 标记                               │
│    4. 确认 Rule.judgment 使用路径不绕过 Authority 层         │
└─────────────────────────────────────────────────────────────┘
```

---

## 七、修复建议

### 7.1 立即行动（Blocker）

```bash
# 1. 证据状态回滚到修改前
git checkout 520a045^ -- data/evidence/blind_seg/

# 2. 或标记为 HOLD（不删除，但标注为待审核）
python scripts/hold_blind_evidence.py
```

### 7.2 修复 Code 问题

```python
# src/tongshu/engines/blind/palace.py
# 移除 confidence 字段
@dataclass(frozen=True)
class PalaceRule:
    palace: str
    semantics: List[str]
    source: str
    # confidence: float = 1.0  ← DELETE THIS
```

### 7.3 建立独立验证流程

```yaml
Evidence Verification Policy:
  allowed_verifiers:
    - "Human Expert (姓名)"
    - "Cross-Reference with Published Text"
  
  prohibited_verifiers:
    - "Hermes Agent"
    - "Blind BOT"
    - 任何自动生成系统
  
  required_for_upgrade:
    - Independent source confirmation
    - Chapter/page locator
    - Verbatim quote comparison
```

---

## 八、后续步骤

1. **HOLD commit**：当前状态不允许进入 production
2. **修复 blocker 问题**：证据状态回滚 + 移除 confidence 字段
3. **重新提交**：分离 Engine 代码和 Evidence 修复为两个 commit
4. **独立验证**：所有 VERIFIED 证据需由 Human Expert 重新确认

---

**报告生成**: 2026-07-20
**状态**: ❌ HOLD — 需修复后重新提交
