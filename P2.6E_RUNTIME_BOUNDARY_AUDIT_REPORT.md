# P2.6-E Runtime Boundary Audit Report

**执行时间**: 2026-09-06
**执行者**: Agnes (子平引擎Agent)
**目标**: 追踪 `calc_five_element_balance` 输出流向，验证 AUXILIARY_SIGNAL 隔离性

---

## 审计结果: 🔴 BOUNDARY VIOLATION

### 发现: `five_element_imbalance` 已越权进入 Signal/Judgment 层

尽管 `calc_five_element_balance` 已明确标记为：
```python
AUTHORITY_STATUS = NOT_AUTHORIZED
ROLE = AUXILIARY_SIGNAL
```

但在以下位置仍直接产生 Judgment 级信号：

---

## 违规点 #1: 盲派引擎产生 HEALTH_ISSUE 信号

**文件**: `src/tongshu/engines/blind_bazi_engine.py:575`

```python
# 第575行
if getattr(chart, 'five_element_imbalance', False) or body_chonged:
    signals.append(CanonicalSignal(
        signal_id=f"blind-health-{birth_year}", 
        source_engine=SourceEngine.BLIND,
        event_type="HEALTH_ISSUE",        # ← 直接产生 Judgment 级事件
        domain=Domain.LIFE_EVENT,
        direction=EventDirection.NEGATIVE,
        layer=SignalLayer.BASELINE))
```

**问题**:
- `five_element_imbalance` 是 ENGINEERING_HEURISTIC 输出
- 但直接触发了 HEALTH_ISSUE 信号
- 违反了 "不得直接进入 Judgment 判断链" 的声明

---

## 违规点 #2: Heluo 引擎五行失衡判断

**文件**: `src/tongshu/reasoning/signal_engine.py:97-103`

```python
# 第97-103行
if benming_wuxing and bazi and getattr(bazi, "five_element_balance", None):
    bazi_key = _HELUO_WUXING_TO_BASI_KEY.get(benming_wuxing)
    if bazi_key and bazi_key in bazi.five_element_balance:
        ratio = bazi.five_element_balance[bazi_key]
        if ratio > _WUXING_OVER_THRESHOLD:
            out["heluo_wuxing_imbalance"] = "over"
        elif ratio < _WUXING_UNDER_THRESHOLD:
            # ...
```

**问题**:
- 直接使用 `five_element_balance` 的比例值
- 应用于 `_WUXING_OVER_THRESHOLD` 和 `_WUXING_UNDER_THRESHOLD` 判断
- 产生 Heluo 引擎的失衡信号

---

## 违规点 #3: 子平 STRENGTH_PATH 使用五行分布

**文件**: `src/tongshu/judgment_architecture/system_school_contract.py:133`

```python
paths["STRENGTH_PATH"] = {
    "school": ZiPingSchool.DI_TIAN_SUI.value,
    "name_zh": "滴天髓 - 强弱/气势路径",
    "input_features": {
        "day_master": features.get("ZP.DAY_MASTER"),
        "month_branch": features.get("ZP.MONTH_BRANCH"),
        "five_element_balance": features.get("ZP.FIVE_ELEMENT_BALANCE"),  # ← 输入
        "branch_clash_map": features.get("ZP.BRANCH_CLASH_MAP"),
        "branch_he_map": features.get("ZP.BRANCH_HE_MAP")
    },
    "judgment_types": ["STRENGTH", "QI_SHI", "STRUCTURE_LEVEL", "STEM_IMAGE"],
    "matcher": "GRAPH"
}
```

**问题**:
- 将 `five_element_balance` 作为滴天髓路径的输入
- 影响 STRENGTH / QI_SHI / STRUCTURE_LEVEL 等 Judgment 判断
- 违反了 "不作为经典计算结果" 的声明

---

## 调用链分析

```
BaziChart.five_element_imbalance
    ↓
[1] blind_bazi_engine.py:575 → HEALTH_ISSUE 信号
    ↓
[2] signal_engine.py:97-103 → heluo_wuxing_imbalance
    ↓
[3] system_school_contract.py:133 → STRENGTH_PATH 输入
    ↓
Judgment Layer (滴天髓强弱判断)
```

---

## 架构影响

### 当前架构 vs 预期架构

**预期架构** (P2.6-D 声明):
```
算 (Calculation)
├── Canonical Calculation → Calculation Authority ✅
└── Engineering Heuristic → AUXILIARY_SIGNAL ⚠️ (仅参考)
                              ↓
                            辨 (Signal Layer)
                              ↓
                             解 (Judgment)
```

**实际架构** (发现违规):
```
算 (Calculation)
├── Canonical Calculation → Calculation Authority ✅
└── Engineering Heuristic → AUXILIARY_SIGNAL ⚠️
                              ↓
                    [越权!] 直接产生 Judgment 信号 ❌
                              ↓
                            辨 (Signal Layer)
                              ↓
                             解 (Judgment)
```

### 违反的原则

1. **"不得声称为由经典授权的计算结果"**
   - 实际：作为滴天髓路径输入，被当作权威信号使用

2. **"不得直接进入 Judgment 判断链"**
   - 实际：直接触发 HEALTH_ISSUE 信号

3. **"不参与 Calculation Freeze 的权威证明"**
   - 实际：影响 STRENGTH_PATH 的判断逻辑

---

## 修复建议

### 方案 A: 完全隔离 (推荐)

1. **移除盲派引擎中的直接使用**
   ```python
   # 修改 blind_bazi_engine.py:575
   # 从:
   if getattr(chart, 'five_element_imbalance', False) or body_chonged:
   # 改为:
   if body_chonged:  # 仅使用体用冲克判断健康
   ```

2. **移除 Heluo 引擎中的直接使用**
   ```python
   # 修改 signal_engine.py:97-103
   # 禁用 five_element_balance 的五行失衡判断
   # 或使用独立的 engineering_signal 字段区分
   ```

3. **修改 STRENGTH_PATH 输入**
   ```python
   # 修改 system_school_contract.py:133
   # 移除 five_element_balance 作为滴天髓路径输入
   # 或标记为 "engineering_reference" 而非 canonical_input
   ```

### 方案 B: 标记为 Engineering Signal (保留但隔离)

1. 新增 `engineering_signals` 字段到 BaziChart
2. `five_element_imbalance` 写入该字段而非直接字段
3. Signal Layer 区分 `canonical_signals` 和 `engineering_signals`
4. Judgment 层仅消费 `canonical_signals`

---

## 结论

**P2.6-D 的降级声明未能在运行时强制执行。**

虽然 docstring 和 metadata 已标记 `five_element_balance` 为 AUXILIARY_SIGNAL，但实际代码中仍在直接产生 Judgment 级信号。

**需要立即修复这 3 处违规，否则降级声明形同虚设。**

---

## 附录: 其他 AUXILIARY_SIGNAL 使用情况

除上述 3 处违规外，以下位置仅读取数据但不产生 Judgment：
- `bazi_engine.py:671` — attach_p2_fields 计算
- `bazi_engine.py:688` — 写入 BaziChart 字段
- `evidence_producer.py:171` — 证据生产（仅记录，不产生信号）
- `feature_registry` — 特征注册（元数据，不影响判断）

---

## 紧急程度

| 违规点 | 严重性 | 建议 |
|--------|--------|------|
| blind_bazi_engine.py:575 | 🔴 HIGH | 立即移除 |
| signal_engine.py:97-103 | 🟡 MEDIUM | 隔离处理 |
| system_school_contract.py:133 | 🔴 HIGH | 移除输入 |
