# 顺天命理 · 断言层架构重构蓝图 V11
> 时间：2026-08-28
> 方法论依据（用户确立，最高原则）：
>   1. **互补不比较** —— 各体系各司其职，从不同维度印证同一命主，不投票、不比较
>   2. **反方向 = 算法错误** —— 体系内在同源，出现反方向即断言层/算法有BUG，当错误排查修复，不设"冲突"状态
>   3. **每个引擎回归本位强项** —— 不被统一"方向"壳束缚

---

## 一、设计哲学（总纲）

命理各体系同源于阴阳五行/天人合一宇宙观，对一个命主的**根本判断内在一致**。
所谓"交叉"不是"比较谁对谁错"，而是**多体系从不同维度互补印证同一件事**：

- 一致 → 信心增强
- 反方向 → **不是冲突，是算法有错** → 触发审计修复

`CONFLICTED` 状态废弃。

---

## 二、各引擎本位定位（回归强项）

| 引擎 | 本位领域 | 擅长输出 | 输出形态（不再是统一方向） |
|---|---|---|---|
| **子平** | 旺衰/格局/用神 | 人生层次、上限、根基、用神取向 | 旺衰判定、格局立格、调候用神、扶抑用神 |
| **紫微** | 星曜/宫位/四化 | 分领域细象（婚姻/财帛/官禄/疾厄各宫） | 命宫主星、12宫吉凶、四化飞布、大限流年 |
| **盲派** | 做功/应期 | 具体事件 + 发生时间 | 做功结构、宾主体用、刑冲合害墓库、应期 |
| **河洛** | 卦象/数理 | 心性、时机、进退、先天/后天卦 | 先天卦、元堂、后天卦、大运流年卦 |
| **易经** | 决策/占卜 | 当下该不该做、怎么选、画险趋吉 | 卦辞爻辞、人间道指引、决策建议 |

**关键转变**：每个引擎输出**自己领域的结构化判断**，而不是压缩成一个"positive/negative"方向。

---

## 三、断言层数据流（新架构）

```
                 ┌───────────── 引擎层（各司其职） ─────────────┐
 排盘           │ 子平→旺衰/格局/用神                           │
(八字/紫微/河洛) │ 紫微→宫位/四化细象                           │
                 │ 盲派→做功/应期                               │
                 │ 河洛→先天/后天卦 + 时机                       │
                 └────────────────┬────────────────────────────┘
                                  │  各引擎输出【结构化本位信号】
                                  ▼
                 ┌───────────── 主题层（互补印证视图）──────────┐
                 │ 事业 / 财运 / 婚姻 / 健康 / 时运              │
                 │ 收集各引擎对该主题【不同角度】的判断           │
                 └────────────────┬────────────────────────────┘
                                  │
                     ┌────────────┴────────────┐
                     ▼                          ▼
              多角度一致                反方向（=算法错误）
              → 信心增强                → 触发算法审计（不进结论）
```

---

## 四、核心数据结构改造

### 1. 废弃：`Confidence.CONFLICTED`
- 删除该枚举值
- 反方向不再产出"冲突断言"，而是产出**审计信号**（见下）

### 2. 新增：`Direction` 按维度拆分
- 各引擎输出**自己领域的多维度判断**，例如紫微：
  ```
  ziwei: {
    ming_gong: {...},     # 命宫主星/格局
    hun_yin: {...},       # 夫妻宫细象
    cai_bo: {...},        # 财帛宫细象
    guan_lu: {...},       # 官禄宫细象
    ji_e: {...},          # 疾厄宫细象
    si_hua: {...},        # 四化飞布
  }
  ```
- 不再统一成一个 `direction`

### 3. 新增：审计信号（`AuditFlag`）
- 当主题层发现多体系在同一主题方向相反时：
  ```
  AuditFlag(
    topic="marriage",
    conflicting_engines=["ziwei: positive", "ziping: negative"],
    hypothesis="其中一个引擎算法有误或维度未对齐",
    action="进入该引擎算法审计",
  )
  ```
- 审计信号**不进最终断言结论**，独立输出，驱动修算法

---

## 五、主题层：互补印证而非投票

### 旧逻辑（删除）
```
pos_score = Σ权重(positive引擎)
neg_score = Σ权重(negative引擎)
pos>neg→方向; 正负都有→CONFLICTED
```

### 新逻辑（互补印证）
```
主题 = 事业/婚姻/财运/健康/时运

1. 收集：各引擎对该主题【本位角度】的判断
   - 婚姻：紫微夫妻宫细象 + 子平配偶星用神 + 盲派配偶星做功 + 河洛卦象
2. 印证：多角度是否共同指向同一结论
   - 全部指向吉/凶 → 信心增强（多角度印证）
   - 方向相反 → 生成 AuditFlag，不输出结论，驱动算法审计
3. 输出：该主题的多维度判断 + 印证度（非单一方向）
```

---

## 六、权重表去向

- 旧的 `SYSTEM_WEIGHTS`（用于投票定方向）**废弃**
- 权重概念保留为：**各体系在主题上的"印证权重"**（该体系对本主题的发言权）
- 作用从"投票定方向"改为"衡量该体系判断对本主题的重要程度"
  ```
  婚姻：紫微0.90（夫妻宫最直接） 子平0.75 盲派0.70 河洛0.60
  ```
- 但**不参与方向表决**，只影响"印证度"计算和审计优先级

---

## 七、实施阶段（后续，不在本次）

1. **P1 契约改造**：删 `CONFLICTED`，`Assertion.direction` 支持多维，新增 `AuditFlag`
2. **P2 各引擎本位化**：逐个引擎回归本位输出（先子平旺衰/格局/用神，再紫微宫位，再盲派做功应期，再河洛卦象）
3. **P3 主题层重写**：从"投票"改"互补印证"，接入 AuditFlag
4. **P4 审计流程**：反方向→定位算法错误→修复

> 本次只做架构整理，**不动代码**。待你确认架构后，再按阶段推进。
