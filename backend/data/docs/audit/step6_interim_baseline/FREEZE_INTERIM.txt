==============================================================
 FREEZE DECLARATION · STEP 6 INTERIM BASELINE
 Date: 2026-08-23 | Tag: baseline-v1.4-interim-20260823
 Commit: 8f3b081fc601d3717db49cee9493c86fea2854a4
 Branch: master
==============================================================

1. FREEZE SCOPE
   The following are frozen as of the above tag and SHALL NOT be
   modified without an explicit governance decision:

   a) Golden Dataset — all docs/golden_cases/GOLDEN-*.yaml files.
      No changing expected values to make tests pass.
   b) GOLDEN_BASELINE.md (7/20 real numbers + 13 failure ledger).
   c) DB state — otcg (55 rules / 52 evidence / 10 mappings) and
      shuntian_kb (115 tables / 1887 rows) are read-only reference
      baselines. No schema changes, no data backfills.
   d) Governance artifacts committed in 8f3b081 (step0–step3 docs).
   e) Ziwei engine — canonical evidence set remains BLOCKED pending
      G-F3 / 梯二规则扩展期望值复核.
   f) Zi-chu (子初) date-rollover policy — routed through time-policy
      adapters (B-02 fix); do not bypass.
   g) NFC feature — offline, do not re-enable without governance.

2. ARTIFACT MANIFEST (snapshot products)
   docs/audit/step6_interim_baseline/
     GIT_STATE.txt                — HEAD/tag/branch/commit chain
     pytest-interim.log           — 1297 passed, 1 skipped
     DB_SNAPSHOT_INTERIM.md       — dual-DB read-only counts
     GOLDEN_RECHECK.md            — golden rerun confirmation (7/20)
     golden-interim.log           — full golden run output
     GATE_INTERIM.txt             — 8-item gate verification (8/8 PASS)
     INTERIM_HASHES.sha256        — SHA-256 chain (this file + others)
     FREEZE_INTERIM.txt           — this declaration
   Included from STEP 0 (in hash chain):
     docs/audit/step0_baseline/GOLDEN_BASELINE.md

3. GATE RESULT
   8/8 PASS — see GATE_INTERIM.txt for per-item evidence.

4. PROHIBITIONS DURING FREEZE
   - NO git add -A / git add . (whitelist discipline continues)
   - NO modifying golden YAML expected values
   - NO DB writes / migrations / schema changes
   - NO re-enabling NFC or bypassing time-policy adapters
   - NO treating "71 tables / 577 rows" as current (HISTORICAL only)

5. NEXT STEPS (outside this task)
   - G-F3: Ziwei canonical evidence set unblock
   - Bazi domain: 梯二规则扩展期望值复核
   - Cross-analysis auto-convergence after above two
   - ~90 scaffold/out-of-scope files: separate governance decision

==============================================================
 END OF FREEZE DECLARATION
==============================================================
