# BOT-ZIPING Phase 2 深度 Rule-level 审计报告

**任务 ID**: T-ENGINE-BAZI-002 Phase 2 Deep Audit  
**执行者**: @bot-ziping  
**日期**: 2026-09-06  
**状态**: 🔴 CRITICAL - 生产级辨证引擎未实现

---

## 执行摘要

深度 Rule-level 审计揭示：**五个辨证代理的 `_load_classic_entries()` 全部为空实现**。证据数据库存在但未被代理使用，规则引擎完全缺失。

### 关键发现

| 组件 | 状态 | 问题 |
|------|------|------|
| 辨证代理基类 | ✅ 框架完整 | 数据结构设计合理 |
| 证据提取接口 | ⚠️ 接口存在 | 无实际实现 |
| 经典数据加载 | 🔴 **完全空实现** | 所有 5 个代理返回 `[]` |
| 证据数据库 | ⚠️ 数据存在 | 代理未连接 |
| 规则应用引擎 | 🔴 **完全缺失** | 无规则链、无优先级 |
| 旺衰计算 | 🔴 **完全缺失** | 无计算逻辑 |
| 格局判定 | 🔴 **完全缺失** | 无判定逻辑 |
| 用神判断 | 🔴 **完全缺失** | 无判断逻辑 |

---

## P0-1：旺衰引擎审计

### 现状分析

**期望的 Rule 链**：
```
月令得令 → 得地（通根）→ 得势（生扶）→ 综合判断 → 强弱结论
```

**实际实现**：
```python
# bian/dts_agent.py:51-54
def _load_classic_entries(self) -> List[Dict]:
    """加载滴天髓原文数据"""
    # TODO: 实现具体加载逻辑
    return []
```

### 发现的问题

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 无月令判断逻辑 | 🔴 P0 | `get_seasonal_support()` 仅生成 Evidence 占位符 |
| 无通根计算 | 🔴 P0 | 未实现藏干→日主通根判定 |
| 无生扶统计 | 🔴 P0 | 未统计印比数量 |
| 无综合评分 | 🔴 P0 | CanonicalState 禁止评分，但无替代规则 |
| 无原典规则加载 | 🔴 P0 | `_load_classic_entries()` 返回空列表 |

### 规则链追溯测试

```python
# 期望的追溯链
月令得令 (DTS-WS-001) → 滴天髓·通神论·衰旺
    ↓
得地通根 (DTS-WS-002) → 滴天髓·根气篇
    ↓
得势帮扶 (DTS-WS-003) → 滴天髓·比劫篇
    ↓
综合判断 (DTS-WS-004) → 滴天髓·衰旺综合论
```

**实际状态**: 4/4 规则节点均无实现

---

## P0-2：格局引擎审计

### 期望的 Rule 链

```
月令定格 → 透干成格 → 生克制化 → 成格/破格判定 → 特殊格局检查
```

### 实际实现检查

```python
# bian/pzzq_agent.py:47-49
def _load_classic_entries(self) -> List[Dict]:
    """加载子平真诠原文数据"""
    return []  # 空实现
```

### 格局判定矩阵

| 格局类型 | 判定条件 | 原典依据 | 状态 |
|----------|----------|----------|------|
| 正官格 | 月令正官透干 | 《子平真诠·论正官》 | ❌ 未实现 |
| 七杀格 | 月令七杀透干 | 《子平真诠·论七杀》 | ❌ 未实现 |
| 财格 | 月令财星透干 | 《子平真诠·论财》 | ❌ 未实现 |
| 印格 | 月令印星透干 | 《子平真诠·论印》 | ❌ 未实现 |
| 食神格 | 月令食神透干 | 《子平真诠·论食神》 | ❌ 未实现 |
| 伤官格 | 月令伤官透干 | 《子平真诠·论伤官》 | ❌ 未实现 |
| 建禄格 | 月支=日主禄位 | 《三命通会·论建禄》 | ⚠️ 数据存在，无判定逻辑 |
| 阳刃格 | 月支=日主帝旺 | 《渊海子平·论阳刃》 | ⚠️ 数据存在，无判定逻辑 |
| 从格 | 日主无根无帮扶 | 《子平真诠·论从格》 | ❌ 完全缺失 |
| 化格 | 天干五合化气 | 《滴天髓·化气》 | ❌ 完全缺失 |

### 关键缺陷

**无优先级判定逻辑**：
```python
# 应有逻辑
if is_congruent_pattern():
    return "从格"  # 最高优先级
elif is_transformation_pattern():
    return "化格"
elif is_jianlu_pattern():
    return "建禄格"
elif is_yangren_pattern():
    return "阳刃格"
else:
    return determine_normal_pattern()  # 正格
```

**实际状态**: 无任何优先级判定代码

---

## P0-3：用神系统审计

### 概念混淆检测

**应有区分**：
```python
USEFUL_GOD_TYPES = {
    "climate": "调候用神 - 寒暖燥湿",      # 《穷通宝鉴》
    "support": "扶抑用神 - 旺衰平衡",      # 《滴天髓》
    "pattern": "格局用神 - 成格要求",      # 《子平真诠》
    "regulation": "制化用神 - 病药平衡",   # 《子平真诠》
}
```

**实际状态**: 
- 无 `USEFUL_GOD_TYPES` 枚举
- 无优先级链定义
- `tiaohou_loader.py` 仅有数据加载，无规则应用

### 调候系统检查

```python
# engines/tiaohou_loader.py
def get_tiaohou(day_stem: str, month_branch: str) -> Optional[dict]:
    """获取调候用神数据"""
    data = _load_all()
    # ... 数据查询
    return data.get(key)
```

**问题**:
- 仅有数据查询，无规则应用
- 未与格局判断关联
- 未与旺衰判断关联

---

## P0-4：十神语义审计

### BAZI → ZIPING 边界检查

**已通过**:
- ✅ 十神计算由 `bazi_ten_gods.ten_god()` 唯一实现
- ✅ ContextAssembler 已改为 import 而非重复实现

**待验证**:
- ❓ 十神组合语义映射
- ❓ 十神位置分析
- ❓ 十神生克关系

### 十神语义链

```
甲日主 + 庚干 → 七杀 (BAZI计算)
七杀是否得用？ → 需旺衰判断 (未实现)
七杀是否有制？ → 需格局判断 (未实现)
七杀成格与否？ → 需格局判断 (未实现)
七杀代表什么？ → 需十神语义映射 (未实现)
```

**实际状态**: 只有第一层（BAZI计算），后续四层均未实现

---

## P0-5：事件判断审计

### 应有规则链

```
命局判断 → 大运作用 → 流年引动 → 作用关系 → 事件信号 → 事件断言
```

### 实际实现

**命局判断**: 无
**大运作用**: 仅有地支冲合检测
**流年引动**: 仅有天干十神计算
**作用关系**: 部分实现（冲合害刑）
**事件信号**: 仅有 DerivedSignal 数据结构
**事件断言**: 完全缺失

### 关键缺失

```python
# 应有规则引擎
class EventJudgmentEngine:
    def judge_marriage(self, ctx: TemporalContext) -> list[Assertion]:
        # 检查配偶星、配偶宫、大运流年作用
        pass
    
    def judge_career(self, ctx: TemporalContext) -> list[Assertion]:
        # 检查官杀、格局、用神
        pass
    
    def judge_wealth(self, ctx: TemporalContext) -> list[Assertion]:
        # 检查财星、身强身弱
        pass
```

**实际状态**: 此类完全不存在

---

## 证据数据库审计

### 数据库状态

| 经典 | 文件数 | 说明 |
|------|--------|------|
| 渊海子平 | 117 | ✅ 存在，有结构化数据 |
| 子平真诠 | ~130 | ⚠️ 部分存在 |
| 滴天髓 | 44 | ⚠️ 部分存在 |
| 穷通宝鉴 | 1200+ | ✅ 存在，调候数据完整 |
| 三命通会 | 8 | ❌ 太少 |

### 数据连接检查

```python
# 检查代理是否正确加载证据
for agent in [YHZP, PZZQ, DTS, QTBJ, SMTH]:
    entries = agent._load_classic_entries()
    print(f"{agent.CLASSIC_NAME}: {len(entries)} entries")
```

**实际输出**:
```
渊海子平: 0 entries  ← 数据库存在但未加载！
子平真诠: 0 entries
滴天髓: 0 entries
穷通宝鉴: 0 entries
三命通会: 0 entries
```

---

## 架构决策建议

### 立即行动（P0）

1. **实现 `_load_classic_entries()`**
   - 连接 `data/evidence/` 下的 JSON 文件
   - 解析并索引证据数据

2. **实现基础规则链**
   - 月令得令判定
   - 通根判定
   - 简单格局判定（正格）

3. **建立证据连接**
   - 将证据数据库连接到辨证代理

### 中期行动（P1）

1. **实现完整规则引擎**
   - 旺衰计算链
   - 格局判定链
   - 用神选择链

2. **实现事件判断引擎**
   - 财运判断
   - 事业判断
   - 婚姻判断
   - 健康判断

### 长期行动（P2）

1. **完善证据覆盖**
   - 补充《三命通会》相关证据
   - 验证现有证据的完整性

2. **建立优先级体系**
   - 格局 > 调候 > 扶抑
   - 特殊格局 > 正格

---

## 最终裁决

| 层级 | 状态 | 说明 |
|------|------|------|
| BAZI → ZIPING 接通 | 🟢 PASS | 数据流正常 |
| ZIPING 不重算 BAZI | 🟢 PASS | 边界清晰 |
| 十神单一权威源 | 🟢 PASS | 已修复 |
| 藏干表 | 🟢 PASS | 单一权威源 |
| 十二长生基础算法 | 🟢 PASS | 正确实现 |
| BAZI 强度边界 | 🟢 PASS | fail-closed 已实现 |
| 旺衰语义审计 | 🔴 **FAIL** | 规则链完全缺失 |
| 格局语义审计 | 🔴 **FAIL** | 规则链完全缺失 |
| 用神/调候/制化 | 🔴 **FAIL** | 无优先级链 |
| 十神组合语义 | 🔴 **FAIL** | 无语义映射 |
| 事件判断 | 🔴 **FAIL** | 完全缺失 |
| 五经 Evidence 权威性 | 🟡 **PARTIAL** | 数据存在但代理未连接 |
| `MODERATE` fallback | ✅ **FIXED** | 已改为 fail-closed |

---

## ZIPING Freeze 裁决

**❌ 不建议冻结**

原因：
1. 五个辨证代理的 `_load_classic_entries()` 全部为空实现
2. 证据数据库存在但未被代理使用
3. 规则引擎完全缺失
4. 事件判断完全缺失
5. 无法进行 Rule-level provenance 追溯

**建议**：
- 先完成 P0-1 到 P0-5 的规则链实现
- 再验证证据连接
- 最后进行 Freeze 裁决

---

**报告完成**: 2026-09-06  
**状态**: 🔴 BLOCKED - 核心规则链未实现  
**发现**: 5个P0问题（核心规则缺失），3个P1建议，2个P2建议

---
*@bot-ziping*
