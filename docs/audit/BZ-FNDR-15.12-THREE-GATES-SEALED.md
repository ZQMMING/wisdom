# 三道实施门封口记录（BZ-FNDR-15.12）

> **BZ-FNDR-15.12：① Event-Signal 最终裁决 + 三门全封 + 施工授权状态登记**
> **Ruling Record Only — Code = 0 / Evidence = 0 / Rule = 0 / Algorithm = 0**
>
> 依据：User 2026-09-10 对 ① 的最终裁决；前置 15.10（双树对账）/ 15.11（②③ 联合基础设施）。
> 执行：BOT-MASTER（User 授权接管 BOT-ZIPING 前置任务）。

---

## 0. 记录元数据

| 项 | 值 |
|---|---|
| 记录 ID | BZ-FNDR-15.12 |
| 前置 | 15.11（`16e1d47e`） |
| ① 裁决 | **严格 FAIL-CLOSED + 独立 ⑮-1-EVENT-SIGNAL 方法审计 + 否决临时白名单** |
| ② / ③ | 已锁（15.11）：②-C + C1a / ③ V3+V4 |
| 三门状态 | **全部封口** |
| ZIP-INT-01~08 | 🔴 未授权（任务单编制中 ≠ 开工授权） |
| G1 | 不修改 |
| 本地 commits | 6 ahead + 本记录 = 7，**不推** |

---

## 1. ① 裁决全文登记（User 原文锁定）

### ①-a SHIJIAN 立即严格 fail-closed

```text
Canonical Bazi Facts → Feature Resolver → Event Signal Resolver → SHIJIAN

Event Method 未审计前:
event_signal = UNKNOWN → SHIJIAN = UNKNOWN → NO deterministic event claim

❌ 禁止语义偷换:
   "没有 event_signal → EVENT_ABSENT"

EVENT_ABSENT 仅在: 事件检测方法已执行、且依据明确规则确认不存在符合条件的事件信号时，
才有资格出现。
```

### ①-b 否决临时事件白名单

```text
❌ 冲 → 某 event_type / 刑 → 某 event_type / 害 / 合 / 空亡
理由: "存在命理关系" ≠ "已证明该关系就是某个事件类型"。
branch_clash 是确定的结构事实（算层）；
"clash → 事业/婚姻/健康/财务事件" 进入辨层语义，必须:
   算: branch_clash_map = X
   辨: 是否构成 Event Signal？依据/适用条件/证据？
   解: 现代语言表达
```

### ①-c 事件证据链 = 独立方法审计为主

```text
Corpus B 检索 = Evidence Provider（证据提供），不是 mapping 生成源。
❌ 不是: Corpus B → 找到"冲刑害合" → 直接建 event_type mapping
✅ 而是:
Canonical Fact → Feature → Candidate Event Signal → Method Definition
→ Classical/engineering evidence → Evidence validation
→ Deterministic resolver → SHIJIAN

原则: 证据支持方法，而不是证据直接充当方法。
```

### ①-d 五经语料检索必须分三类证据

| 类 | 证明什么 |
|---|---|
| ① Classical semantic evidence | 某命理结构在经典体系中具有某种含义 |
| ② Method evidence | 在什么条件下，该结构如何进入判断 |
| ③ Event mapping | 该判断如何合法转换成 SHIJIAN 的 Event Signal / Event Type |

**只有 ③ 成立才能进入 Event Resolver；①不得冒充③。**（与 DTS-106 = ENGINEERING-DEFINED 裁决一致。）

### ①-e SHIJIAN 状态正式定义（七态表）

| 条件 | SHIJIAN |
|---|---|
| Event Method 未审计 | `UNKNOWN` |
| 已审计但输入不足 | `UNKNOWN` |
| 已执行且证据不足 | `UNKNOWN` |
| 已执行且确认事件 | `EXECUTED / conclusion` |
| 已执行且确认无事件 | `EXECUTED / EVENT_ABSENT` |
| Stage 未连接 | `NOT_EXECUTED` |
| Stage 失败 | `FAILED` |

**强调：`EVENT_ABSENT` ≠ "当前没有 event signal"；必须有实际执行、可验证的事件检测方法支持。**

### ①-f BLIND event_type 禁用为 ZiPing Oracle

```text
BLIND event_type → ❌ 不得作为 ZiPing Event Type Oracle。
两引擎语义体系独立。同名概念（clash/harm/combine/...）也必须分别证明:
ZiPing method + ZiPing evidence + ZiPing resolver。
不能因另一引擎已实现，就认为 ZiPing 获得方法依据。
```

---

## 2. 三门封口总表（本记录封档时点）

```text
① Event-Signal    🔒 严格 FAIL-CLOSED
                  🔒 独立 ⑮-1-EVENT-SIGNAL 方法审计
                  ❌ 临时白名单
                  ❌ BLIND 借用
② Evidence Loader 🔒 C + C1c ✅ + C1a (data/evidence = Corpus Base)
③ verification    🔒 V3 + V4
────────────────────────────────────────────
基础设施与方法层完整链路:
Corpus B → Index → Provenance Resolver
→ Canonical Facts → Features → Rules → Event Method → SHIJIAN
→ JudgmentClaimComposer → AC-ZP-* → G1
```

## 3. 施工授权状态（关键结论登记）

```text
三道实施门全部裁决 = 方法论与基础设施封口
但 ≠ 授权施工。
ZIP-INT-01~08  = 🔴 未授权
任务单编制     = 授权中（BOT-MASTER，User 指令）
任务单 ≠ 开工授权。
硬前置（写入任务单）:
  ⑮-1-EVENT-SIGNAL 方法审计 → PASS → 才允许 Event Resolver 实施
  禁止在施工中夹带未经审计的 Event Mapping
```

## 4. 边界与冻结

```text
✅ Code/Evidence/Rule/Algorithm Change = 0
✅ 15.6c / 15.7 / 15.8 / 15.9 / 15.10 / 15.11 不回改
✅ G1 不修改；backend/data/evidence 降级状态维持（15.11 §1.3 禁止项有效）
✅ 本地 7 commit 全部不推（a719bc53 / 14b0b313 / a6933e9b / 7928e1e8 / f89b2166 / 16e1d47e / 本记录）
```

*Generated by BOT-MASTER on 2026-09-10*
*Code: 0 | Evidence: 0 | Rule: 0 | Algorithm: 0 — single-file ruling record*
