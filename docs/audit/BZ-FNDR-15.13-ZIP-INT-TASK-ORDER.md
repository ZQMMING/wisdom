# ZIP-INT-01~08 实施任务单（BZ-FNDR-15.13）

> **BZ-FNDR-15.13：P0-1 施工任务单编制（含 ②③① 三门裁决的全部前置与约束）**
> **Document Only — Code = 0 / Evidence = 0 / Rule = 0 / Algorithm = 0**
>
> ⚠️ **任务单 ≠ 开工授权。** 本记录仅编制任务；ZIP-INT-01~08 每项的实际施工
> 仍需 User 逐项授权。任务单编制完成时点：三门全封（①FAIL-CLOSED / ②C1a / ③V3+V4），
> 但 **⑮-1-EVENT-SIGNAL 方法审计尚未执行**，故 ZIP-INT-04 的事件部分处于硬前置未解锁状态。

---

## 0. 任务单元数据

| 项 | 值 |
|---|---|
| 任务单 ID | BZ-FNDR-15.13 |
| 依据 | 15.6c（D-1~D-7）/ 15.7（ZIP-PRE 01/02/04 + DTS-106）/ 15.8（X-1a）/ 15.9 / 15.10 / 15.11 / 15.12 |
| 执行主体 | BOT-ZIPING（或 User 授权的代执行者） |
| 管辖边界 | `src/tongshu/reasoning/`（ZiPing 辨层）/ `src/tongshu/pipeline_stages/`（新 Stage）/ `src/tongshu/pipeline.py`（接线）；**禁入** 八字冻结域、其他引擎目录、Golden Dataset |
| 提交纪律 | 单引擎单 commit；审计文档不上 GitHub；每 commit 后回 BOT-MASTER 复核 |

## 1. 全局前置条件（Gate 0 — 全部满足才允许开工）

```text
G0-1  ② Evidence Index 实施设计定稿（基于 15.11 §1.4 契约:
      Stable Resource ID + Logical URI + 相对路径 + Runtime Resolver;
      Corpus Base = data/evidence; 树A 降级 Legacy Snapshot）
G0-2  ③ Resolved Provenance 计算层定义定稿（V3+V4: 原始状态保留、解析态另行计算、
      大小写归一在 Resolved 层、claim 携带 PROVENANCE-PENDING 标记的 schema 扩展）
G0-3  ⑮-1-EVENT-SIGNAL 方法审计 完成且 PASS（①-a: SHIJIAN 在此之前严格 fail-closed，
      仅解锁 ZIP-INT-04 的 event resolver 部分）
G0-4  P0-2 状态模型（JudgmentStatus 3 态 × EngineStatus 4 态正交）实施授权 —
      若未授权, ZIP-INT-01/07 先按"仅 CONNECTED/DEGRADED 出 claim"最小语义施工(D-7),
      状态枚举重构另走 ZIP-STATE-01~03
```

## 2. 任务条目（ZIP-INT-01~08）

| ID | 任务 | 对应裁决 | 修复项 | 硬约束 |
|---|---|---|---|---|
| **ZIP-INT-01** | 新增 `ZiPingJudgmentStage`（辨层） | D-3/D-1 | N-1 | 入口 `require_factory=True`（15.6b N-1 订正：bridge 现状 False 属 P1，新 Stage 不得继承）；Stage 内 Domain Status 五域；异常不吞（禁 G-1 式包装） |
| **ZIP-INT-02** | Feature Resolver：域隔离 Feature View | D-4 | N-2 | 命局 8 字段按域切分（five_element_balance→旺衰 / day_branch_main_ten_god→格局·旺衰 / clash·sanxing·harm·he·sanhe map→事件·合局 / kong_wang→旺衰·事件）；**luck_pillars 不进 V1 命局五域**；"进 Feature ≠ 全域可读" |
| **ZIP-INT-03** | `build_context` 重定义为正式 Feature/Context Contract | D-4/N-2 | N-2 | 禁裸 dict（现状只取 4柱+日主）；契约字段 = ②-D4 域 Feature View 投影 |
| **ZIP-INT-04** | `signals_by_domain` 接入 | D-5 / ①-a | C-2 | **事件部分 G0-3 未 PASS 前禁实施**；未解锁面只许 SHIJIAN=UNKNOWN（fail-closed），禁"无信号→EVENT_ABSENT"语义偷换；非事件类 signal 通道可先行（仍走确定性 resolver，禁 LLM） |
| **ZIP-INT-05** | 唯一 `JudgmentClaimComposer` | D-2 / X-1a | F-7/G-5 | claim 命名空间 **`AC-ZP-{domain}-{conclusion}`**（⚠️ 15.6c 原 `ZC-*` 已由 X-1a 废弃，本任务单以 15.8 为准）；`composer_version` 审计 provenance；UNKNOWN/NOT_EXECUTED/FAILED 不出确定性 claim；claim 携带 ③-V3 provenance 标记（与 G0-2 联动）；**不改 G1** |
| **ZIP-INT-06** | ComputeStage 4a 拆分（`run_compute` + `compose_sir`） | D-3-4a | B-3/C-4/D-3/F-4 | SIR 定稿移到 Judgment 完成后；禁"Compute 顺手生成最终 SIR"（4b 永久否决） |
| **ZIP-INT-07** | PipelineResult 挂 `ziping.engine_status` | D-1 | H-2/H-3 | **不挂**完整 JudgmentSynthesis；5 域 Domain Status 留 Stage 内部；fail-closed：NOT_CONNECTED/FAILED = 0 claim |
| **ZIP-INT-08** | 链 A 旧 `ZI_PING` claims 审计处理 | D-2② | 双轨 | 审计现有 ZI_PING claims 语义归属 → 降级/移除其"ZiPing 最终判断 claim"身份；ZiPing Judgment = ZiPing 生产判断唯一权威源；审计记录先行（0 改动），处置需 User 拍板 |

## 3. 施工顺序（锁死）

```text
Stage 0:  G0-1 / G0-2 基础设施定稿（②③ 实施设计 + 任务单内小项）
Stage 1:  ZIP-INT-01 → 02 → 03   （Stage 主体 + Feature 契约）
Stage 2:  ZIP-INT-06               （4a 拆分，SIR 合流点成型）
Stage 3:  ZIP-INT-05               （Composer 接入 SIR → G1 唯一 Gate）
Stage 4:  ⑮-1-EVENT-SIGNAL 方法审计 PASS（G0-3）
           → ZIP-INT-04 event 部分解锁
Stage 5:  ZIP-INT-07               （PipelineResult 状态面 + 全链回放）
Stage 6:  ZIP-INT-08               （旧 claims 处置，User 拍板后）
Stage 7:  enable_ziping_claims 开关（默认 OFF 起步；默认 ON 前须 G1 全解析 + E8 确定性回放）
```

## 4. 每项退出标准（DoD 通用）

```text
✅ 单引擎单 commit，CHANGED FILES 边界内（BOT-MASTER 跑 git show --name-only 复核）
✅ 目标测试套件全 PASS + 边界/负例断言（fail-closed 路径必须有测试）
✅ 0 P0 路径独立性残留（生产代码不得硬编码绝对路径）
✅ 不触碰: G1 逻辑 / Golden Dataset / 八字冻结域 / 其他引擎 / 15.6c 已锁架构
✅ commit 后回报 BOT-MASTER: CHANGED FILES / TESTS / COMMIT
```

## 5. 与 DTS-106 / Q1/Q2/Q3 的交叉边界

```text
ZIP-INT-05 实施 DTS-106 相关 claim 时: 按 15.7 §4 S1-S8（ENGINEERING-DEFINED）执行,
  不得引用伪经典 Evidence / 不得标 CLASSICAL-PROVEN / G1 provenance 不伪装经典。
Q1（12 组同 ID 多文件）: 在 G0-1 Index 实施设计内解（Logical URI 消歧）;
  COMPLEX_WORK-002 ID 错配 ❌ 不在任何 ZIP-INT 项内顺手修 → Evidence Identity 审计单独授权。
Q2（E-DTS-145-001）/ Q3（1440 schema）: 与 ZIP-INT 全部正交, 不混入。
```

## 6. 授权状态

```text
三门:        ① 🔒 / ② 🔒 / ③ 🔒   （15.12）
任务单:      ✅ 已编制（本记录）
开工授权:    🔴 未授予 — G0-1/G0-2 定稿 + User 对 ZIP-INT 各 Stage 的施工授权
             （建议授权粒度: 按 Stage 分批授权, 禁止一次性全开）
```

*Generated by BOT-MASTER on 2026-09-10 — task order document, no construction authorization*
