# G0 Integration Audit（BZ-FNDR-15.15）

> **BZ-FNDR-15.15：G0 接入审计 —— Evidence Corpus B → Index → Logical URI/Resolver
> → Resolved Provenance → 现有 RuleLoader/G1 全链核验（User 7 验证点）**
> **Audit-only / Code=0 / Evidence=0 / Rule=0 / Algorithm=0 / 单文件记录 / 本地 commit / 不推送**
>
> 前置：User 2026-09-10 裁决「G0 施工验收 PASS，但先做 G0 Integration Audit 再进 ZIP-INT」。
> 本记录**不重审 G0、不改代码**，只回答："这条链是否已安全接入 ZiPing 真实生产链？"

---

## 0. 审计元数据

| 项 | 值 |
|---|---|
| 审计 ID | BZ-FNDR-15.15 |
| 审计对象 | `1ca93627`（G0-1）+ `6e3896ee`（G0-2）在 HEAD 的生产接入状态 |
| 取证方式 | 全仓 grep import 面 / `git show HEAD:` 只读 / relocation 实跑实验 / 既有测试回归 |
| Code/Evidence/Rule/Algorithm Change | **0** |
| Git push | **不推**（本地领先 origin/main 增至 10 commit） |

---

## 1. 七验证点逐项结果

### VP-1 G0 是否被生产代码消费？—— **坐实：当前 0 生产消费者（孤立模块）**

```text
grep "from tongshu.governance" 全 src + scripts + tests:
  命中 = tests/test_g0_1 / tests/test_g0_2（仅测试自身）
生产链引用面:
  src/tongshu/pipeline.py / pipeline_stages/*.py / reasoning/*.py
  → 0 引用 governance
```

**定性（如实披露，非缺陷）**：G0 施工范围按 15.13 任务单本就是 **Stage 0 基础设施**，
接入生产是 ZIP-INT-01/06 的职责。审计结论 = **G0 处于"就绪未接线"状态，与任务单设计一致；
但"模块级 PASS"确实尚未证明生产接入安全 —— 接入安全性由本审计 VP-2~VP-7 承担。**

### VP-2 RuleLoader 是否仍绕过 Index？—— **坐实：是，仍直扫树A 根级**

```text
git show HEAD:_rule_backends.py L74:  self._evidence_dir.glob("*.json")（非递归）
pipeline.py L147: data_dir = repo_root / "backend" / "data"  → 树A
```

**现状定性**：生产可见证据面 = 树A 根级 86 资源（15.7 C-2a 结论不变）；
树B Corpus Base 目前**仅经 G0 Index 可达，未进生产**。树A→树B 的语料切换
（C1a 的生效点）**尚未发生** —— 见 §2 过渡态登记。

### VP-3 Resolver 是否改变既有 Claim 行为？—— **否（当前 0 影响面）**

```text
ResolvedProvenance / PROVENANCE-PENDING / claim_markers 全仓生产链引用 = 0
既有 claim 生产链: compute_stage / audit_composer（Chain A）→ G1
G1 不读 verification_status（15.7 C-2c 坐实）→ G0 上线后 G1 行为 0 变化
⇒ 旧链行为不变；新链标记在 Composer（ZIP-INT-05）落地前无生产效力
```

### VP-4 authority_type / source_layer 是否贯通？—— **Index 侧贯通 ✅ / 生产消费侧待 Composer**

```text
Index:  raw_authority / raw_source_layer 原值保留（字段 + iter_raw_statuses 暴露）✅
G1:    只读 claim 级 source_layers（非空校验, L40）; 不读资源级 authority_type（15.7）
⇒ 贯通面在 G0 内闭环; 权威判定的生产消费点 = 未来 JudgmentClaimComposer（15.11 §3.1）
```

### VP-5 relocation 后 Logical URI → Resolver 是否成立？—— **实跑 PASS**

```text
实验: 把 2 个代表资源复制到 C:\Windows\Temp\<new>\relocated_corpus 再 build Index
  evid://relocated/di_tian_sui/E-DTS-106-001.json → <new>\...\di_tian_sui\E-DTS-106-001.json ✅
  evid://relocated/E-ZPZ-101-001.json             → <new>\...\E-ZPZ-101-001.json ✅
⇒ Identity 稳定（evid:// 前缀只含 tree 标签, 不含部署路径）;
   物理定位完全跟随 corpus_root 注入, 0 硬编码 —— 满足 15.11 §1.4 "换机/迁移不变" 契约
```

### VP-6 G0 是否改变 ⑮-2 provenance 结果？—— **否**

```text
judgment.py / context_assembler.py / compute_stage.py 对 governance 引用 = 0
既有回归: test_ziping_15_2_evidence_provenance.py 11/11 PASS
         + test_p0_evidence_chain.py          → 合计 20/20 PASS
⇒ ⑮-2 CITATION/_Citations 面行为不变
```

### VP-7 G1 不动下, 旧链/新链是否出现双重 authority？—— **当前无; 过渡态风险已登记**

```text
现状: 生产唯一权威链 = RuleLoader(树A根级) → G1（evidence_ids 解析面, g1 L36-39）
      G0 链（树B Index → ResolvedProvenance）0 生产效力 ⇒ 无双头
风险: ZIP-INT 接入 + 树A→树B 语料切换若分两步实施,
      切换窗口内会出现"两个不同可见证据集"短暂并存
      （G1 解析面=树A, Composer 面=树B）→ 可能产生引用可解析性漂移
登记: 语料切换必须【原子单步】完成（Index corpus_root 一次切到树B,
      同步更新 RuleLoader data_dir 指向, 同一 commit / 同一 PR 面）,
      列入 ZIP-INT 实施任务单硬约束（见 §3）。
```

---

## 2. 审计总判定

```text
VP-1 就绪未接线   ✅（与设计一致, 非缺陷）
VP-2 旧链未切     ✅ 坐实（树A 直扫面不变）
VP-3 无行为漂移   ✅（0 生产消费面）
VP-4 贯通闭环     ✅（Index 侧完成, 消费侧待 Composer）
VP-5 relocation   ✅ 实跑通过
VP-6 ⑮-2 不变    ✅（20/20 回归）
VP-7 无双头       ✅ 现状 + ⚠️ 过渡态风险登记（原子切换约束）

⇒ G0 Integration Audit = PASS（有条件）
   条件 = ZIP-INT 接入时执行 §3 的原子切换约束
⇒ P0-1 ZIP-INT 现在【可以】进入施工（User 门: 审计 PASS 为前置, 本记录满足）
```

## 3. 交回 ZIP-INT 施工的新增硬约束（本审计唯一产出物）

```text
T-1  树A→树B 语料切换（RuleLoader data_dir + Index corpus_root）必须原子单步
     （同一 commit 面, 禁止"先切 Index 后切 Loader"或反之的中间态上线）
T-2  JudgmentClaimComposer（ZIP-INT-05）落地时, claim provenance 标记
     唯一来源 = ResolvedProvenance（15.11 §3.1 分工, 禁双源标记）
T-3  语料切换后首跑必须重放: G1 全解析面 + test_p0_evidence_chain +
     test_ziping_15_2_evidence_provenance（VP-6 基线 20/20 不得漂移）
```

## 4. 边界与冻结

```text
✅ Code/Evidence/Rule/Algorithm Change = 0（VP-5 relocation 实验在临时目录, 未入库）
✅ G1 / judgment.py / signal_engine.py / RuleLoader 全部未触碰
✅ SHIJIAN 冻结面未动; ZIP-INT 未开工（本记录是其前置）
✅ 本地 10 commit 不推; 推送前 User 反向核验 SHA/parent/changed-files 维持
```

*Generated by BOT-MASTER on 2026-09-10*
*Code: 0 | Evidence: 0 | Rule: 0 | Algorithm: 0 — single-file integration audit record*
