# P2.5 子平 Calculation Integrity Audit

**审计时间**: 2026-09-05
**审计范围**: BaziEngine 完整计算路径
**审计目标**: 证明子平"算什么、怎么算、算出来是否正确，以及每个计算结果为什么有资格存在"

---

## 一、计算路径追踪

### 1.1 顶层架构

```
Input (birth_date, hour, timezone, location, gender)
    ↓
TimeResolver (T4 - 时间归一化)
    ↓
ResolvedBirthInstant (effective_date, effective_hour)
    ↓
BaziAdapter (投影层)
    ↓
BaziEngine.compute()
    ↓
BaziChart (L1 Facts + P2 Fields)
    ↓
Signal Layer (待实现)
    ↓
Bian Layer (辨 - 待实现)
    ↓
Jie Layer (解 - 待实现)
```

### 1.2 各层职责边界

| 层 | 文件 | 职责 | 证据要求 |
|----|------|------|----------|
| T4 TimeResolver | `engines/time/resolver.py` | 真太阳时、子初换日、时区校正 | 历法原典 |
| Adapter | `engines/bazi_adapter.py` | CalculationContext → BaziChart 投影 | 无新计算 |
| Engine | `engines/bazi_engine.py` | 四柱计算、关系识别、L1 Facts | 五书Evidence |
| L1 Facts | `engines/bazi_l1_facts.py` | 十二长生、藏干（原始数据） | implementation_source |
| Signal | `signal/` | 语义信号（待实现） | 禁止混入 |
| Bian | `bian/` | 辨层（待实现） | 禁止混入 |
| Jie | `jie/` | 解层（待实现） | 禁止混入 |

---

## 二、历法与四柱计算审计

### 2.1 TimeResolver 职责确认

**文件**: `src/tongshu/engines/time/resolver.py`

**已实现功能**:
- [ ] 真太阳时校正 (`longitude_offset`)
- [ ] 均时差校正 (`equation_of_time`)
- [ ] 子初换日 (23:00 boundary)
- [ ] IANA timezone 解析
- [ ] LocationRegistry 查表

**需核验问题**:
1. 是否存在重复换日？(TimeResolver + BaziEngine 双重换日风险)
   - **现状**: BaziAdapter 传 `skip_late_zi=True` 避免双重换日 ✓
   - **风险**: 直接调用 `BaziEngine.compute()` 时若 `skip_late_zi=False` 会双重换日

2. 节气计算是否准确？
   - **现状**: 依赖 `sxtwl` 库，未独立实现
   - **风险**: sxtwl 权威性需确认

### 2.2 四柱计算审计

**年柱**: 使用 sxtwl `getYearGZ()`
- **证据**: 暂无独立Evidence
- **状态**: ⚠️ 待溯源

**月柱**: 使用 sxtwl `getMonthGZ()`
- **证据**: 暂无独立Evidence  
- **状态**: ⚠️ 待溯源

**日柱**: 使用 sxtwl `getDayGZ()`
- **证据**: 暂无独立Evidence
- **状态**: ⚠️ 待溯源

**时柱**: 使用 sxtwl `getHourGZ(hour, True)`
- **证据**: 暂无独立Evidence
- **状态**: ⚠️ 待溯源

**简化算法** (`_compute_simple`):
- 年柱: `(year - 4) % 60` 映射
- 月柱: 基于年干推算月干公式
- 日柱: 基于1900-01-01偏移量计算
- 时柱: `hour_branch()` + `hour_stem_from_day_stem()` (五鼠遁)

### 2.3 计算契约定义

#### 年柱计算契约
```text
RULE: 年柱 = getYearGZ(solar_date)
INPUT: (year, month, day)
NORMALIZATION: 无（直接使用公历年份）
ALGORITHM: sxtwl.fromSolar(year, month, day).getYearGZ()
OUTPUT: Pillar(stem, branch)
INVARIANTS: 
  - stem ∈ HEAVENLY_STEMS
  - branch ∈ EARTHLY_BRANCHES
  - 60甲子循环
EVIDENCE: [待补充]
NOT: 不包含节气边界判断（年柱以立春为界，需确认sxtwl实现）
```

#### 月柱计算契约
```text
RULE: 月柱 = getMonthGZ(solar_date)
INPUT: (year, month, day)
NORMALIZATION: 需确认是否已考虑节气
ALGORITHM: sxtwl.fromSolar(year, month, day).getMonthGZ()
OUTPUT: Pillar(stem, branch)
INVARIANTS:
  - branch = (month + 1) % 12 (正月建寅)
  - stem 基于年干推算
EVIDENCE: [待补充]
NOT: 不包含月令旺衰判断
```

#### 日柱计算契约
```text
RULE: 日柱 = getDayGZ(solar_date)
INPUT: (year, month, day)
NORMALIZATION: 无
ALGORITHM: sxtwl.fromSolar(year, month, day).getDayGZ()
OUTPUT: Pillar(stem, branch)
INVARIANTS:
  - 60甲子循环
  - 日柱不随时间变化（仅日期）
EVIDENCE: [待补充]
NOT: 不包含日主旺衰判断
```

#### 时柱计算契约
```text
RULE: 时柱 = getHourGZ(hour, True)
INPUT: (year, month, day, hour)
NORMALIZATION: hour 已转为真太阳时
ALGORITHM: sxtwl.fromSolar(year, month, day).getHourGZ(hour, True)
OUTPUT: Pillar(stem, branch)
INVARIANTS:
  - branch = hour_branch(hour)
  - stem 基于日干五鼠遁
EVIDENCE: 见 STEM_HE 相关Evidence
NOT: 不包含时辰吉凶判断
```

---

## 三、十神计算审计

### 3.1 当前实现

**文件**: `src/tongshu/engines/bazi_engine.py:320-364`

```python
def _ten_god(day_master: str, other: str) -> str:
    """计算其他天干对日主的十神关系."""
    dm_el = STEM_ELEMENT[day_master]
    ot_el = STEM_ELEMENT[other]
    same = STEM_POLARITY[day_master] == STEM_POLARITY[other]
    
    if dm_el == ot_el:
        return "比肩" if same else "劫财"
    if _GENERATES.get(dm_el) == ot_el:
        return "食神" if same else "伤官"
    if _GENERATES.get(ot_el) == dm_el:
        return "偏印" if same else "正印"
    if _CONTROLS.get(ot_el) == dm_el:
        return "七杀" if same else "正官"
    if _CONTROLS.get(dm_el) == ot_el:
        return "偏财" if same else "正财"
    raise ValueError(...)
```

### 3.2 计算契约

```text
RULE: 十神 = f(日主, 他干/支)
INPUT: day_master (stem), other (stem or element)
NORMALIZATION: 提取五行属性 + 阴阳属性
ALGORITHM: 
  - 同五行同阴阳 → 比肩
  - 同五行异阴阳 → 劫财
  - 日主生他 → 食神(同)/伤官(异)
  - 他生日主 → 偏印(同)/正印(异)
  - 他克日主 → 七杀(同)/正官(异)
  - 日主克他 → 偏财(同)/正财(异)
OUTPUT: Literal["比肩","劫财","食神","伤官","偏印","正印","七杀","正官","偏财","正财"]
INVARIANTS:
  - 输出必须为10种十神之一
  - 同一对输入必须返回相同结果（确定性）
EVIDENCE: [需从五书提取十神定义原文]
NOT: 
  - 不代表吉凶
  - 不代表旺衰
  - 不代表强弱
  - 仅代表关系分类
```

### 3.3 边界检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 十神输出是否越界进入语义判断？ | ✅ 通过 | 仅返回字符串标签 |
| 是否包含吉凶词汇？ | ✅ 通过 | 无"吉"/"凶"等词 |
| 是否包含旺衰判断？ | ✅ 通过 | 无"旺"/"弱"等词 |
| 输入是否包含位置信息？ | ⚠️ 需确认 | 当前仅接受stem，未区分宫位 |

---

## 四、藏干与十二长生审计

### 4.1 当前实现

**文件**: `src/tongshu/engines/bazi_l1_facts.py`

#### 十二长生

```python
TIAN_GAN_TWELVE_GROWTH = {
    "甲": {"子": "沐浴", "丑": "冠带", ...},
    "乙": {"子": "病", "丑": "衰", ...},
    # ...
}
```

**关键发现**:
- `己土` 条目标注为 **"P0-1.3：己土保持原表，UNRESOLVED"**
- 数据来源声明为 `IMPLEMENTATION_SOURCE`（非 Canonical Source）
- 体系声明：阳顺阴逆，火土同生

**状态评估**:
| 天干 | 状态 | 说明 |
|------|------|------|
| 甲 | ✅ 已证实 | 阳干顺行 |
| 乙 | ✅ 已证实 | 阴干逆行 |
| 丙 | ✅ 已证实 | 阳干顺行 |
| 丁 | ✅ 已证实 | P0-1.3修正，阴干逆行 |
| 戊 | ✅ 已证实 | 与丙同论 |
| 己 | ⚠️ UNRESOLVED | 标注明确，需后续裁决 |
| 庚 | ✅ 已证实 | 阳干顺行 |
| 辛 | ✅ 已证实 | 阴干逆行 |
| 壬 | ✅ 已证实 | 阳干顺行 |
| 癸 | ✅ 已证实 | 阴干逆行 |

#### 藏干

```python
BRANCH_HIDDEN_STEMS = {
    "子": {"本气": "癸", "中气": None, "余气": None},
    "丑": {"本气": "己", "中气": "癸", "余气": "辛"},
    # ...
}
```

**状态评估**: 全部12地支均有完整定义，无UNRESOLVED标注。

### 4.2 计算契约

#### 十二长生查询契约
```text
RULE: 十二长生 = lookup(day_master, branch)
INPUT: stem (天干), branch (地支)
NORMALIZATION: 无
ALGORITHM: TIAN_GAN_TWELVE_GROWTH[stem][branch]
OUTPUT: Literal["长生","沐浴","冠带","临官","帝旺","衰","病","死","墓","绝","胎","养"]
INVARIANTS:
  - 输出必须为12阶段之一
  - 己土输入可能返回UNRESOLVED标记
EVIDENCE: implementation_source (非canonical)
NOT:
  - 不代表旺衰
  - 不代表吉凶
  - 仅表示天干在地支的能量状态标签
```

#### 藏干查询契约
```text
RULE: 藏干 = lookup(branch)
INPUT: branch (地支)
NORMALIZATION: 无
ALGORITHM: BRANCH_HIDDEN_STEMS[branch]
OUTPUT: dict {本气: stem, 中气: stem|null, 余气: stem|null}
INVARIANTS:
  - 本气必须非空
  - 中气/余气可为空
EVIDENCE: implementation_source (非canonical)
NOT:
  - 不代表有根/无根
  - 仅代表地支包含的天干成分
```

---

## 五、地支关系计算审计

### 5.1 六合计算

**常量**: `BRANCH_HE`
**Evidence**: `E-YHZP-005-001`
**计算函数**: `calc_branch_he_map()`

```text
RULE: 六合 = 检查四支是否存在BRANCH_HE定义的配对
INPUT: 四支列表
ALGORITHM: 遍历所有pair，检查是否在BRANCH_HE中
OUTPUT: dict {pair_key: [branch1, branch2, element]}
INVARIANTS:
  - he(A,B) == he(B,A) (对称性)
  - 同一对只能出现一次
  - 不处理争合情况
EVIDENCE_SUPPORT: 渊海子平E-YHZP-005-001
DOES_NOT_SUPPORT: 
  - 合化条件判断
  - 合化结果判定
  - 吉凶判断
```

### 5.2 三合计算

**常量**: `BRANCH_SANHE`
**Evidence**: `E-YHZP-006-001`
**计算函数**: `calc_branch_sanhe_map()`

```text
RULE: 三合 = 检查四支是否包含BRANCH_SANHE定义的三元组
INPUT: 四支列表
ALGORITHM: 检查每个triple是否是四支的子集
OUTPUT: dict {triple_key: [branches..., element]}
INVARIANTS:
  - 需要三支齐全才算成局
  - 不处理半合、暗合
EVIDENCE_SUPPORT: 渊海子平E-YHZP-006-001
DOES_NOT_SUPPORT:
  - 三合化气条件
  - 三合成局的效力判断
```

### 5.3 三会计算

**常量**: `BRANCH_SANHUI`
**Evidence**: `E-DTS-145-001`
**计算函数**: 未在bazi_engine.py中发现显式计算函数

```text
RULE: 三会 = [待实现]
STATUS: ⚠️ 常量已定义，但无对应计算函数
ACTION: 需确认是否需要实现
```

### 5.4 三刑计算

**常量**: `BRANCH_SANXING`
**Evidence**: `E-YHZP-007-001`
**计算函数**: `calc_branch_sanxing_map()`

```text
RULE: 三刑 = 检查四支是否包含BRANCH_SANXING定义的组合
INPUT: 四支列表
ALGORITHM:
  - 检查三支齐全的三刑（寅巳申、丑戌未）
  - 检查两支的无礼之刑（子卯）
  - 检查自刑（同一地支出现两次以上）
OUTPUT: dict {xing_key: [branches..., xing_name]}
INVARIANTS:
  - 自刑需同一地支出现≥2次
  - 不处理刑的效力判断
EVIDENCE_SUPPORT: 渊海子平E-YHZP-007-001
DOES_NOT_SUPPORT:
  - 刑的吉凶判断
  - 刑的解除条件
```

### 5.5 六冲计算

**常量**: `BRANCH_CLASH`
**Evidence**: `E-YHZP-002-001`
**计算函数**: `calc_branch_clash_map()`, `calc_day_branch_clash()`

```text
RULE: 六冲 = 检查四支是否存在BRANCH_CLASH定义的冲关系
INPUT: 四支列表
ALGORITHM: 遍历所有pair，检查是否在BRANCH_CLASH中
OUTPUT: dict {pair_key: [branch1, branch2]}
INVARIANTS:
  - clash(A,B) == clash(B,A) (对称性)
  - 按位置识别，不按值过滤（已修复e568d9f）
EVIDENCE_SUPPORT: 渊海子平E-YHZP-002-001
DOES_NOT_SUPPORT:
  - 冲的效力判断
  - 冲去冲起判断
  - 吉凶判断
```

### 5.6 六害计算

**常量**: `BRANCH_HARM`
**Evidence**: `E-YHZP-003-001`
**计算函数**: `calc_branch_harm_map()`, `calc_day_branch_harm()`

```text
RULE: 六害 = 检查四支是否存在BRANCH_HARM定义的害关系
INPUT: 四支列表
ALGORITHM: 遍历所有pair，检查是否在BRANCH_HARM中
OUTPUT: dict {pair_key: [branch1, branch2]}
INVARIANTS:
  - harm(A,B) == harm(B,A) (对称性)
  - 按位置识别
EVIDENCE_SUPPORT: 渊海子平E-YHZP-003-001
DOES_NOT_SUPPORT:
  - 害的效力判断
  - 吉凶判断
```

### 5.7 桃花计算

**常量**: `PEACH_BLOSSOM_BY_DAY`, `PEACH_BLOSSOM_DIRECT`
**Evidence**: `E-YHZP-004-001`
**计算函数**: `calc_peach_blossom()`

```text
RULE: 桃花 = 日支是否在PEACH_BLOSSOM_DIRECT中
INPUT: BaziChart
ALGORITHM: chart.day_pillar.earthly_branch in PEACH_BLOSSOM_DIRECT
OUTPUT: bool
INVARIANTS:
  - 仅判断日支
  - 不判断年支/月支/时支桃花
EVIDENCE_SUPPORT: 渊海子平E-YHZP-004-001
DOES_NOT_SUPPORT:
  - 桃花的吉凶判断
  - 桃花的分类（墙内/墙外）
```

### 5.8 空亡计算

**常量**: `KONG_WANG_BY_XUN`
**Evidence**: `E-YHZP-008-001`
**计算函数**: `calc_kong_wang()`

```text
RULE: 空亡 = 根据日柱旬查表
INPUT: BaziChart
ALGORITHM:
  - 计算日柱在60甲子中的序号
  - 除以10得到旬索引
  - 查KONG_WANG_BY_XUN表
OUTPUT: tuple (空亡地支1, 空亡地支2)
INVARIANTS:
  - 每旬固定两个空亡
  - 不处理填实、冲实等动态变化
EVIDENCE_SUPPORT: 渊海子平E-YHZP-008-001
DOES_NOT_SUPPORT:
  - 空亡的效力判断
  - 空亡的填补条件
```

---

## 六、P2 字段边界审计

### 6.1 BaziChart 字段分类

| 字段 | 类型 | 证据支持 | 状态 |
|------|------|----------|------|
| `spouse_star` | ⚠️ 语义信号 | 无 | **需迁移** |
| `spouse_star_attack` | ⚠️ 语义信号 | 无 | **需迁移** |
| `officer_mixed` | ⚠️ 语义信号 | 无 | **需迁移** |
| `day_branch_clash` | ✅ 计算事实 | E-YHZP-002-001 | ✓ 保留 |
| `day_branch_harm` | ✅ 计算事实 | E-YHZP-003-001 | ✓ 保留 |
| `spouse_star_strength` | ⚠️ 语义信号 | 无 | **需迁移** |
| `peach_blossom` | ✅ 计算事实 | E-YHZP-004-001 | ✓ 保留 |
| `branch_clash_map` | ✅ 计算事实 | E-YHZP-002-001 | ✓ 保留 |
| `branch_harm_map` | ✅ 计算事实 | E-YHZP-003-001 | ✓ 保留 |
| `branch_he_map` | ✅ 计算事实 | E-YHZP-005-001 | ✓ 保留 |
| `branch_sanhe_map` | ✅ 计算事实 | E-YHZP-006-001 | ✓ 保留 |
| `branch_sanxing_map` | ✅ 计算事实 | E-YHZP-007-001 | ✓ 保留 |
| `kong_wang` | ✅ 计算事实 | E-YHZP-008-001 | ✓ 保留 |
| `five_element_balance` | ⚠️ 待分类 | 无 | **需裁决** |
| `day_branch_main_ten_god` | ✅ 计算事实 | [需补充] | ✓ 保留 |

### 6.2 需要迁移的字段

以下字段明显属于"语义信号"而非"计算事实"：

1. **`spouse_star`**: 
   - 计算的是"配偶星强度"
   - 隐含假设：男命以财为妻，女命以官为夫
   - 这是命理学约定，不是纯数学计算
   - **建议**: 迁移到 Signal Layer

2. **`spouse_star_attack`**:
   - 判断"劫财是否克正财"、"官杀是否混杂"
   - 这是命理判断，不是计算事实
   - **建议**: 迁移到 Signal Layer

3. **`officer_mixed`**:
   - 判断女命是否"官杀混杂"
   - 这是命理判断
   - **建议**: 迁移到 Signal Layer

4. **`spouse_star_strength`**:
   - 基于spouse_star的强度档位
   - 复合判断
   - **建议**: 迁移到 Signal Layer

5. **`five_element_balance`**:
   - 五行分布比例计算是数学事实
   - 但"失衡"标记 (`five_element_imbalance`) 是阈值判断
   - **建议**: 保留分布计算，移除失衡标记或移到Signal

### 6.3 需要补充证据的字段

1. **`day_branch_main_ten_god`**:
   - 计算日支主气藏干对日主的十神
   - 无对应Evidence
   - **建议**: 补充Evidence或使用十神计算的通用Evidence

---

## 七、Evidence → Calculation Traceability Matrix

| Engine Rule | Calculation | Evidence | 原文支持 | 条件完整 | 状态 |
|-------------|-------------|----------|----------|----------|------|
| STEM_HE | 天干五合配对 | E-DTS-144-001 | ✓ | 🟡 部分 | SEMANTIC_MATCHED |
| BRANCH_CLASH | 地支六冲关系 | E-YHZP-002-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| BRANCH_HARM | 地支六害关系 | E-YHZP-003-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| PEACH_BLOSSOM | 桃花查法 | E-YHZP-004-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| BRANCH_HE | 地支六合关系 | E-YHZP-005-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| BRANCH_SANHE | 地支三合关系 | E-YHZP-006-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| BRANCH_SANHUI | 地支三会关系 | E-DTS-145-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| BRANCH_SANXING | 地支三刑关系 | E-YHZP-007-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| KONG_WANG | 空亡旬表 | E-YHZP-008-001 | ✓ | ✓ | SEMANTIC_MATCHED |
| 年柱计算 | sxtwl.getYearGZ() | ❌ 无 | ❌ | ❌ | **UNRESOLVED** |
| 月柱计算 | sxtwl.getMonthGZ() | ❌ 无 | ❌ | ❌ | **UNRESOLVED** |
| 日柱计算 | sxtwl.getDayGZ() | ❌ 无 | ❌ | ❌ | **UNRESOLVED** |
| 时柱计算 | sxtwl.getHourGZ() | ❌ 无 | ❌ | ❌ | **UNRESOLVED** |
| 十神计算 | _ten_god() | ❌ 无 | ❌ | ❌ | **UNRESOLVED** |
| 十二长生 | TIAN_GAN_TWELVE_GROWTH | ❌ 无 | ⚠️ implementation_source | 🟡 己土UNRESOLVED | **PARTIAL** |
| 藏干 | BRANCH_HIDDEN_STEMS | ❌ 无 | ⚠️ implementation_source | ✓ | **PARTIAL** |

---

## 八、Property/Invariant Tests

### 8.1 对称性测试

```python
def test_clash_symmetry():
    """六冲关系必须对称"""
    for a, b in BRANCH_CLASH.items():
        assert BRANCH_CLASH[b] == a, f"Clash not symmetric: {a}-{b}"

def test_harm_symmetry():
    """六害关系必须对称"""
    for a, b in BRANCH_HARM.items():
        assert BRANCH_HARM[b] == a, f"Harm not symmetric: {a}-{b}"

def test_he_symmetry():
    """六合关系必须对称（通过frozenset）"""
    for pair in BRANCH_HE.keys():
        assert len(pair) == 2
```

### 8.2 自反性测试

```python
def test_no_self_clash():
    """地支不能与自己冲"""
    for b in EARTHLY_BRANCHES:
        assert BRANCH_CLASH.get(b) != b, f"Self-clash detected: {b}"

def test_no_self_harm():
    """地支不能与自己害"""
    for b in EARTHLY_BRANCHES:
        assert BRANCH_HARM.get(b) != b, f"Self-harm detected: {b}"
```

### 8.3  completeness测试

```python
def test_all_branches_covered():
    """所有地支都必须出现在关系中"""
    covered = set()
    for a, b in BRANCH_CLASH.items():
        covered.add(a)
        covered.add(b)
    for a, b in BRANCH_HARM.items():
        covered.add(a)
        covered.add(b)
    # 六合、三合、三刑、三会类似检查
    assert covered == set(EARTHLY_BRANCHES), f"Missing branches: {set(EARTHLY_BRANCHES) - covered}"
```

### 8.4 位置独立性测试

```python
def test_clash_position_independent():
    """冲关系判断不应依赖位置顺序"""
    branches1 = ["ZI", "WU", "MAO", "YOU"]
    branches2 = ["WU", "ZI", "YOU", "MAO"]
    # 两组不同顺序，但相同关系集合
    result1 = calc_branch_clash_map(create_chart(branches1))
    result2 = calc_branch_clash_map(create_chart(branches2))
    assert set(result1.keys()) == set(result2.keys())
```

---

## 九、Boundary Audit 结论

### 9.1 通过项

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 十神计算不涉及吉凶词 | ✅ | 仅返回关系标签 |
| 关系计算不含旺衰判断 | ✅ | 纯数学关系 |
| skip_late_zi 避免双重换日 | ✅ | BaziAdapter正确传递 |
| 证据引用链闭环 | ✅ | 9/9常量有evidence_id |
| Layer 2层级保持 | ✅ | SEMANTIC_MATCHED未越级 |

### 9.2 需迁移项

| 字段 | 当前层 | 建议层 | 理由 |
|------|--------|--------|------|
| spouse_star | BaziChart | Signal | 含命理约定 |
| spouse_star_attack | BaziChart | Signal | 含命理判断 |
| officer_mixed | BaziChart | Signal | 含命理判断 |
| spouse_star_strength | BaziChart | Signal | 复合判断 |
| five_element_imbalance | BaziChart | Signal | 阈值判断 |

### 9.3 待补充证据项

| 计算规则 | 优先级 | 建议Evidence来源 |
|----------|--------|------------------|
| 年柱计算 | P0 | 需从五书提取干支纪年原文 |
| 月柱计算 | P0 | 需从五书提取月建原文 |
| 日柱计算 | P0 | 需从五书提取干支纪日原文 |
| 时柱计算 | P0 | 需从五书提取时辰原文 |
| 十神定义 | P0 | 子平真诠《论十干配合性情》 |
| 十二长生 | P1 | 需从滴天髓/三命通会提取 |
| 藏干定义 | P1 | 需从五书提取 |

### 9.4 UNRESOLVED项

| 项目 | 状态 | 说明 |
|------|------|------|
| 己土十二长生 | ⚠️ UNRESOLVED | bazi_l1_facts.py明确标注 |
| 年/月/日/时柱原典证据 | ❌ 缺失 | sxtwl库权威性未溯源 |
| 十神原典证据 | ❌ 缺失 | 仅实现算法，无Evidence引用 |

---

## 十、下一步行动

### 立即行动（本周期）

1. **迁移P2语义字段** → 创建Signal Layer或暂时标记为"待迁移"
2. **补充十神Evidence** → 从子平真诠提取十神定义原文
3. **添加Property Tests** → 对称性、自反性、完备性测试

### 后续行动（下周期）

1. **溯源四柱计算** → 确认sxtwl权威性，或补充五书原典Evidence
2. **裁决己土十二长生** → 等待人类专家签字或明确标记NOT_AUTHORIZED
3. **完善Evidence Mapping** → 确保每个计算规则都有对应的Atomic Evidence

---

**审计人**: P2.5 Calculation Integrity Audit
**日期**: 2026-09-05
**状态**: 🟡 初步审计完成，待用户裁决迁移方案
